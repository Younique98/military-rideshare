{
    "name": "firestore-rules-tests",
    "private": true,
    "description": "Firestore security rules test suite for Base Link, run against the Firebase emulator (not part of the deployed app).",
    "scripts": {
        "test": "node rules.test.js"
    },
    "devDependencies": {
        "@firebase/rules-unit-testing": "^5.0.0",
        "firebase": "^12.0.0"
    }
}
